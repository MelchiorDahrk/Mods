local ssqn = include("SSQN.interop") --Must come before function calls
if (ssqn) then
    ssqn.registerQIcon("mdMT","\\Icons\\mdMT\\q\\quest_mortalties.tga") --using several underscores in quest ID
end